# --- Ядро сайта (нужно всегда) ---
streamlit>=1.36,<2.0
numpy>=1.26
pillow>=10.0
requests>=2.31
reportlab>=4.0        # генерация PDF-отчётов (чистый Python)

# --- Работа с изображениями (по мере надобности потока B) ---
# scikit-image>=0.22   # раскомментировать, если понадобится обработка
# opencv-python-headless>=4.9

# --- Тесты ---
pytest>=8.0
